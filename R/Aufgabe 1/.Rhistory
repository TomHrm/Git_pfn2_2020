f <- "blast_clupea_harengus.out"
nr <- nrow(b_data)
b_data <- read.table(file = f, skip = 3, col.names = names)
myname <- "AllerBesteCoolsteGruppe"
b_data <- read.table(file = f, skip = 4, col.names = names
xlab_text = expression(paste("log"["10"], italic(' e'), '-value'))
b_data <- read.table(f, skip = 4, col.names = names)
b_data <- read.table(file = f, skip = 3, col.names = names)
b_data <- read.table(file = f, skip = 3,col.names = names)
vec <- c(log(b_data$e_val, base = 10))
b_data <- read.table(file = f, skip = 4,col.names = names)
b_data <- read.table(file = f, skip = 1,col.names = names)
vec <- log(b_data$e_val, base = 10)
b_data <- read.table(file = f, skip = 1, col.names = names)
xlab_text = expression(paste("log"["10"], italic(' e'), '-value'))
dev.off()
f <- 'blast_clupea_harengus.out'
